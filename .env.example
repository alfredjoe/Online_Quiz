# Example .env.example
# Copy this file to `.env` (local) and fill values. Do NOT commit `.env`.

# Frontend (Vite) variables — prefixed with VITE_
VITE_SUPABASE_URL=
VITE_SUPABASE_KEY=
VITE_PDF_EXTRACTOR_URL=
VITE_AVATAR_SERVICE=
VITE_GPTENGINEER_CDN=

# MathPix / OCR (server-side) — keep secrets out of frontend builds
MATHPIX_API_BASE=
MATHPIX_APP_ID=
MATHPIX_APP_KEY=

# Notes:
# - Leave values empty in this example file. Provide real values in a local
#   `.env` (which should be gitignored) or in your deployment platform's
#   secret/variable settings.
# - `VITE_` variables are embedded at build time for the frontend. Keep
#   secrets (like MathPix app key) on the server or in edge functions.
