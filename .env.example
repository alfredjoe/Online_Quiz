# Set the PDF extractor backend base URL (no trailing slash required)
# For local development:
VITE_PDF_EXTRACTOR_URL=http://localhost:5000

# For deployed backend (example):
# VITE_PDF_EXTRACTOR_URL=https://onlinequizbackend-production.up.railway.app

# Optional: MathPix credentials for local server (if using MathPix in server)
# MATHPIX_APP_ID=your_mathpix_app_id
# MATHPIX_APP_KEY=your_mathpix_app_key
